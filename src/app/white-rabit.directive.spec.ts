import { WhiteRabitDirective } from './white-rabit.directive';

describe('WhiteRabitDirective', () => {
  it('should create an instance', () => {
    const directive = new WhiteRabitDirective();
    expect(directive).toBeTruthy();
  });
});
